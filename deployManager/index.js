//code should get events from github and build & deploy the scrapper
//as well as setup any events in the eventbridge the scheduling
exports.handler = async (event, opt) => {
    return {
        msg:"test"
    }
};